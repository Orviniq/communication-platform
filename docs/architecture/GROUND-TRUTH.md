# Ground truth — the measured facts of this deployment

Every entry below carries the date its measurement ran. **Re-verify any entry
older than 90 days.** An entry past that window is a claim, not a fact; re-verify
it or delete it, and never lengthen the window to bring one back inside it.

Scope note, and the reason several sections are thin: the VPS serves from
2026-09-07 and carries no traffic. The system is at scale band 0, with no real
users and an empty production database. Most entries here were measured on the
developer machine, against the repository at the commit the row's date belongs
to — `32617e7` for the rows of phase 4, `e987f28` for the rows of phase 5 run 14.
A row that names the VPS and says `configured` states what the committed operator
configuration sets rather than what a running host reports, and says so; a row
that says `observed on the running host` was read back off the deployment itself,
and those begin at 2026-09-07 with the first one.

Decisions, rationale and rejected alternatives live in
[`DESIGN-RECORD.md`](DESIGN-RECORD.md) and in [`decisions/`](decisions/). This
file holds no decision.

## 1. Topology

| Component | Where it runs | What it reaches | verified |
|---|---|---|---|
| nginx | VPS, ports 80 and 443, TLS 1.3 only, `ssl_early_data off`. Every `location` states its own `client_max_body_size`, equal to the largest body it admits, over a server-level `1k` deny-by-default; `/api/` waits 150 s, above the application's own 120 s deadline. nginx is the sole owner of `Strict-Transport-Security` for the host, through `proxy_hide_header` in `snippets/proxy-headers.conf` | `127.0.0.1:8000` for `/api/`, `/ws` and the admin path | 2026-09-05 (configured, `backend/ops/nginx/chat.orviniq.com.conf` and `backend/ops/nginx/snippets/proxy-headers.conf`; the header and cap behaviour measured against a running nginx 1.31.2 — §4) |
| ASGI application | VPS, uvicorn on `127.0.0.1:8000`, systemd unit `chat.service` (writes `media_root` and nothing else, `LimitNOFILE` 4096, `--limit-concurrency` 1024, `--timeout-graceful-shutdown` 130 under `TimeoutStopSec=150`), user `deploy`, `WEB_CONCURRENCY` workers (default 1), uvloop and httptools, the `websockets` sans-io implementation. In-process: FastAPI is the root application for every scope — every API route, the `/ws` gateway, and the Django ASGI application answering `ADMIN_PATH` alone | PostgreSQL and Redis on loopback | 2026-09-05 (configured, `backend/ops/systemd/chat.service`; composition observed in `backend/config/asgi.py`) |
| PostgreSQL 18 | VPS, loopback — `listen_addresses` reads `localhost` off the running server. **18 and not the 16 this row carried until 2026-09-07**: the host runs Ubuntu 26.04, which packages no PostgreSQL 16, and an APT source outside the distribution is a foreign dependency this project does not take ([0012](decisions/0012-pinned-hashed-and-untracked-wheel-cache.md) is the same argument for wheels). Every migration in the tree applies to it unchanged | — | 2026-09-07 (observed on the running host: `migrate` clean, `showmigrations` reporting nothing unapplied, and the logging posture of §3 read back with `pg_settings`) |
| Redis 8 | VPS, loopback, `bind 127.0.0.1`, `protected-mode yes`, `requirepass` set at deploy time — `check --deploy` refuses a `REDIS_URL` without a password (`core.E004`), and an unauthenticated `PING` answers `NOAUTH`. **8 and not the 7 this row carried until 2026-09-07**, for the reason the PostgreSQL row above gives. `maxmemory 134217728`, `maxmemory-policy noeviction`, `appendonly no` and an empty `save`, every one read back with `CONFIG GET` rather than from the file that set it | — | 2026-09-07 (observed on the running host; `backend/ops/redis/redis-chatapp.conf`, the check in `backend/core/checks.py`) |
| CPython | 3.14 on the VPS, 3.12 on the developer machine. **The runbook asked for 3.12 until 2026-09-07**; Ubuntu 26.04 packages only 3.14, and the constraint that decides it is the wheel cache rather than the interpreter: all 30 pinned production distributions resolve to a `cp314` or `abi3` wheel under `--only-binary=:all: --require-hashes`, `psycopg`, `pydantic-core`, `uvloop`, `httptools`, `websockets`, `cffi` and `argon2-cffi-bindings` included, so the offline install needs no compiler and no source distribution on either | — | 2026-09-07 (observed on the running host; `ops/vendor.sh`'s own command, 52 wheels for the dev set) |
| coturn | VPS, UDP and TCP 3478 and nothing else — no TLS listener and no certificate — relay ports 50000–51000, `use-auth-secret`, `no-stun`, `user-quota` 20 and `total-quota` 500, every private, carrier-grade-NAT, link-local, loopback and unique-local range denied as a peer, and `log-file=/dev/null` with `no-stdout-log` and `simple-log`. The only media service on the host ([0021](decisions/0021-relayed-webrtc-mesh-and-no-server-room.md)); the backend reads `TURN_STATIC_AUTH_SECRET` from this release, and `POST /api/v1/me/relay` mints the credential a client presents here, so the relay is no longer inert | the participants' media | 2026-09-05 (configured, `backend/ops/coturn/turnserver.conf`; pinned by `core/tests/test_settings_posture.py::CoturnPostureTests`) |
| Maintenance job | VPS, systemd timer `chat-maintenance.timer`, hourly with a 5-minute randomised delay and `Persistent=true`; the run is bounded at `TimeoutStartSec=15min` and refuses an overlapping sweep through a PostgreSQL advisory lock | PostgreSQL | 2026-09-05 (configured, `backend/ops/systemd/`; the lock in `backend/messaging/management/commands/prune.py`) |
| Developer machine | macOS, PostgreSQL 16 and Redis 7 native on loopback, venv at `backend/.venv` | — | 2026-09-03 (observed) |

The deployment root on the VPS is `/srv/chat`. The application working directory
is `/srv/chat/backend`. There is no second application host, no load balancer, no
CDN, no external push service, no external STUN, and no third-party API on the
runtime path.

## 2. Deviations from a default configuration

| Setting | The value here | The default | The ADR that decided it | verified |
|---|---|---|---|---|
| `WSGI_APPLICATION` | `None` | the generated `config.wsgi.application` | — (ASGI-only since inception) | 2026-09-03 |
| `AUTH_USER_MODEL` | `accounts.User` | `auth.User` | — (set at inception; a one-way door) | 2026-09-03 |
| `PASSWORD_HASHERS[0]` | `Argon2PasswordHasher` | `PBKDF2PasswordHasher` | — | 2026-09-03 |
| `AUTH_PASSWORD_VALIDATORS` | minimum length 10, common-password check only | four validators, minimum length 8 | — | 2026-09-03 |
| `DB_CONN_MAX_AGE` | 0 | 0 | [0005](decisions/0005-django-orm-on-a-thread-sensitive-data-path.md) | 2026-09-04 |
| `DATABASES["default"]["OPTIONS"]["pool"]` | `{min_size: 1, max_size: 16, timeout: 10}` | no pool | [0005](decisions/0005-django-orm-on-a-thread-sensitive-data-path.md) | 2026-09-04 |
| FastAPI `openapi_url`, `docs_url`, `redoc_url` | `None` in every mode, so the three routes — and the `/docs/oauth2-redirect` route FastAPI adds beside them — are absent from the application everywhere, `DEBUG` included | `/openapi.json`, `/docs`, `/redoc`, always registered | [0008](decisions/0008-fastapi-generates-the-openapi-document.md), closed in every mode by [0020](decisions/0020-one-android-client-and-no-browser-surface.md) | 2026-09-05 |
| FastAPI `generate_unique_id_function` | the route's handler name | the route name, the path and one method taken from an unordered set | [0008](decisions/0008-fastapi-generates-the-openapi-document.md) — the default moves a client's method name when a path moves, and gives one identifier to both operations of a two-method route | 2026-09-04 |
| The `422` FastAPI declares on every route that takes a parameter | removed from the generated document, with the two components only it referenced | declared, referencing `HTTPValidationError` | [0007](decisions/0007-contract-conventions.md) replaced the validation handler, so no route can produce that body | 2026-09-04 |
| FastAPI `router.redirect_slashes` | `False` | `True` | [0007](decisions/0007-contract-conventions.md) | 2026-09-04 |
| Response headers on the API surface | `Cache-Control: no-store` and nothing else. `X-Content-Type-Options` and `Referrer-Policy` are set on no API response, and `Content-Disposition` on no attachment download | Django's `SecurityMiddleware` sets the first two on the responses Django serves, and a download route usually names a disposition | [0020](decisions/0020-one-android-client-and-no-browser-surface.md) — all three instruct a browser, and the one client is a Flutter application. The admin path is unaffected: Django serves it and sets both headers there | 2026-09-05 |
| FastAPI `router.default` | a dispatcher that hands `ADMIN_PATH` (and `STATIC_URL` under `DEBUG`) to the Django ASGI application and raises the router's own 404 for everything else | Starlette's own 404 | [0002](decisions/0002-fastapi-as-the-only-http-api-surface.md) | 2026-09-04 |
| Multipart limits on the upload route | `max_files=1`, `max_fields=0`, and a spool threshold of 64 KiB, set by parsing the form in the route rather than declaring an `UploadFile` parameter | `max_files=1000`, `max_fields=1000`, and a spool threshold of 1 MiB | [0014](decisions/0014-process-hardening-at-the-edge.md) | 2026-09-04 |
| Access log, application | `--no-access-log` on uvicorn, and the `uvicorn`, `uvicorn.error`, `uvicorn.access`, `websockets` and `push_response` loggers claimed in `LOGGING` at WARNING with `propagate: False` | an access log is written, and redis-py installs a stdout `StreamHandler` for `push_response` | [0019](decisions/0019-the-system-emits-no-request-scoped-telemetry.md) | 2026-09-04 |
| Access log, edge | `access_log off` and `error_log … crit` in **both** nginx server blocks | the packaged `nginx.conf` sets an `access_log` in the `http` block, which every `server` block inherits unless it overrides it, in the `combined` format — `$remote_addr` and `$request`, so the client address and the whole URI. `error_log` defaults to `error`, the level at which an upstream failure is written with `client:` and `request:` fields | [0019](decisions/0019-the-system-emits-no-request-scoped-telemetry.md) — found and closed by the phase-4 reviews, which is why this row is separate from the one above | 2026-09-04 (configured, `backend/ops/nginx/chat.orviniq.com.conf`; pinned by `core/tests/test_settings_posture.py`) |
| WebSocket push handler on every `PubSub` the bus opens | `realtime.bus._keep`, which returns the push unchanged | redis-py's own, which logs the topic and the payload at DEBUG and installs the stdout handler above | — (invariant: no identifier, and no blob, reaches a log line) | 2026-09-04 |
| `ThreadSensitiveContext` on a WebSocket scope | not entered; every socket's ORM work shares one executor thread | a context per connection, and therefore a thread per connection | [0005](decisions/0005-django-orm-on-a-thread-sensitive-data-path.md) | 2026-09-04 |
| Redis persistence | `save ""`, `appendonly no` | RDB snapshots on | — (invariant: volatile data never touches disk) | 2026-09-03 |
| `CACHES` | Django's default, `LocMemCache`, which nothing in the project uses | the same default, but a Redis-backed cache is the usual choice when Redis is already present | [0018](decisions/0018-redis-is-authenticated-and-never-deserialized.md) — every built-in cache backend unpickles what it reads, and Redis is writable by every local process | 2026-09-04 |
| `SESSION_COOKIE_NAME`, `CSRF_COOKIE_NAME` (production) | `__Host-sessionid`, `__Host-csrftoken` | `sessionid`, `csrftoken` | — (the VPS serves two other projects; a `__Host-` cookie cannot be set by a sibling site or shadowed by a broader one) | 2026-09-04 |
| `ssl_early_data` | `off` | `off` in nginx, but commonly turned on with TLS 1.3 | — (0-RTT payloads are replayable) | 2026-09-03 |
| `client_max_body_size` | per location: `70m` on `/api/`, `16k` on the admin path, `1k` on `/ws`, `/static/`, `/_protected_attachments/`, `= /` and the server block | 1m, at whatever level it is written | — (each equals the largest body its location admits: `/api/` is `BODY_CAP_BATCH_BYTES`, the admin path claims no FastAPI route so it takes the fallback `BODY_CAP_JSON_BYTES`, and the rest carry no body at all. One server-level number would be the largest location's cap applied to every smaller one) | 2026-09-05 (configured; measured against a running nginx — §4) |
| `proxy_request_buffering` on `/api/` | `on`, stated | `on`, inherited | — (the value is nginx's default; stating it is the deviation, because the application's 120 s deadline is only correct while nginx absorbs the body first. Measured — §4) | 2026-09-06 |
| `client_body_timeout` on `/api/` | 30s | 60s | — (once nginx owns the whole body read, the bound on a client that stalls mid-body is at the edge and nowhere else; a live slow link trickles and never approaches 30 s between two reads) | 2026-09-06 |
| `proxy_read_timeout`, `proxy_send_timeout` on `/api/` | 150s | 60s | — (`UPLOAD_DEADLINE_SECONDS` is 120 s, so nginx's default answered `504` before the application could answer its own `503 unavailable` envelope) | 2026-09-05 |
| `--timeout-graceful-shutdown`, `TimeoutStopSec` | 130 s and 150 s | unset in uvicorn (an unbounded wait); systemd's `DefaultTimeoutStopSec` is 90 s | — (the three windows nest: longest admitted request 120 s < drain 130 s < stop 150 s. Measured — §4) | 2026-09-05 |
| `Strict-Transport-Security` owner | nginx alone, for every response on the host. `proxy_hide_header` drops the copy Django's `SecurityMiddleware` sets on the admin path, and `SECURE_HSTS_SECONDS` stays set there because `check --deploy` requires it (`security.W004`) | both layers emit it, and `add_header` appends rather than replaces, so a proxied response carries it twice | — (measured, before and after — §4) | 2026-09-05 |
| `X-Forwarded-For` at the edge | `$remote_addr`, overwriting | `$proxy_add_x_forwarded_for`, which keeps the client's own list and appends the measured address | — (uvicorn 0.52.4 reads the rightmost entry it does not trust, so both behave the same today; overwriting leaves one value in the header, so no later change to that selection rule can hand the anonymous limiter an address a caller chose. It does not close AR-6) | 2026-09-05 |
| `ssl_protocols` | `TLSv1.3` | `TLSv1.2 TLSv1.3` | — | 2026-09-03 |
| coturn `no-stun` | set | STUN is served | — (relay-only; no discovery surface) | 2026-09-05 |
| coturn `no-tls`, `no-dtls`, and no `tls-listening-port` | set; no certificate path of any kind | a TLS listener on 5349 with a certificate | [0021](decisions/0021-relayed-webrtc-mesh-and-no-server-room.md) — the hop already carries SRTP the relay cannot open, so a listener of its own would put a certificate and a renewal on a box whose posture is to hold neither. It also closes the last public-CA path in the deployment | 2026-09-05 |
| coturn `log-file` | `/dev/null`, with `no-stdout-log` and `simple-log` and no `syslog` | a rotating file under `/var/log/turnserver/`, and the journal by way of stdout | — (invariant 4: a TURN log line names the relay peer pair, which is two devices in one call. `simple-log` is what stops coturn appending a date to the name and writing a real file beside the sink) | 2026-09-05 |
| coturn `user-quota` / `total-quota` | 20 and 500, against a relay range of 1001 ports | unset — unlimited allocations | — (band 0: nine relay allocations for one device in a ten-participant mesh, five such rooms at once for the host, and one allocation takes one relay port, so the range has to exceed the total) | 2026-09-05 |
| coturn `denied-peer-ip=::` | Not set. It was committed in phase 6 and removed in phase 7, because coturn stores a single address as a range whose min and max are that address and `ioa_addr_in_range` returns a match whenever that max is the all-zero "any" address — so the line denied every peer of every family, IPv4 included, and no call could have connected. Measured three ways — §4 | a reader expects the companion of `denied-peer-ip=::1`: the IPv6 unspecified address denied and nothing else. Nothing in coturn's own documentation corrects that reading | — (a defect rather than a decision; the posture it broke is [0021](decisions/0021-relayed-webrtc-mesh-and-no-server-room.md), and `core/tests/test_settings_posture.py::CoturnPostureTests` now fails on any denied range that ends at an all-zero address) | 2026-09-05 |
| uvicorn `--ws-ping-interval`, `--ws-ping-timeout` | 240 s and 60 s | 20 s and 20 s | [0024](decisions/0024-peer-state-published-limits-and-no-activity-dates.md) — a ping every twenty seconds wakes the client's mobile radio every twenty seconds, and the socket is this product's only push path. The consequence is stated rather than hidden: a dead peer holds a socket for at most 300 s — the interval, plus the timeout before the pong is given up on — where it was at most 40 s. That is inside the band: `--limit-concurrency` is 1024 and band 0 is 500 devices. The nginx `/ws` `proxy_read_timeout` is 3600 s, above the interval, so an idle socket is closed by the keepalive and never cut by the edge; the client's own four-minute keepalive stays the client's decision | 2026-09-06 (configured, `backend/ops/systemd/chat.service`; pinned by `core/tests/test_settings_posture.py`) |
| `THROTTLE_ACCOUNTS` | `300/min` | `120/min`, the value every other account-scope route carried since [0010](decisions/0010-redis-rate-limiting-that-fails-closed.md) | [0024](decisions/0024-peer-state-published-limits-and-no-activity-dates.md) — a 50-member fan-out with per-user reads reached 120 in one cycle; the peer-state route removes most of those reads and the raise covers the rest | 2026-09-06 |
| The seven activity dates | **gone.** `Device.last_active_date`, `Device.spk_updated_date`, `Device.pq_spk_updated_date`, `UserIdentity.updated_date`, `ProfileBlob.updated_date`, `KeyBackup.updated_date` and `DeviceLogRecord.stored_date` were nullable with no writer from phase 9; phase 10 run 08 dropped the columns. A dump of these tables has no field a date could be read from. `Device.created_date` and `Device.revoked_date` are the only dates a device row still carries | `auto_now` or `auto_now_add` on a Django date column, which stamps it on every save | [0024](decisions/0024-peer-state-published-limits-and-no-activity-dates.md) | 2026-09-06 |
| PostgreSQL `log_min_error_statement`, `log_error_verbosity` | `panic` and `terse` | `error` and `default` | [0026](decisions/0026-host-posture-and-the-identity-backup.md) — at the defaults a failing statement is written to the server log in full, and a statement of this schema carries device ids, envelope ids and bucketed ciphertext; the `DETAIL` line beside it names the conflicting key values. Measured both ways — §4. PostgreSQL was the one layer of this deployment still writing request data to disk by default | 2026-09-06 (configured, `backend/ops/postgres/README.md`; read back off a running server by `backend/ops/audit/postgres_posture.sh`, which the runbook §8 runs after every deploy) |
| PostgreSQL `log_statement`, `log_min_duration_statement`, `log_min_duration_sample`, `log_duration`, `log_connections`, `log_disconnections`, `log_lock_waits`, `log_min_messages` | `none`, `-1`, `-1`, `off`, `off`, `off`, `off`, `warning` | the same values | [0026](decisions/0026-host-posture-and-the-identity-backup.md) — each is already the PostgreSQL default and each is stated anyway, because a default is not a decision: an operator's earlier edit, a restored `postgresql.conf` or a package upgrade moves one silently and nothing in this repository would report it. They are the row above's other paths to the same disk | 2026-09-06 (configured; checked by the same script) |
| `messaging_queuedenvelope` storage parameters | `autovacuum_vacuum_scale_factor = 0.01`, `autovacuum_vacuum_threshold = 100` | 0.2 and 50, from the cluster | [0026](decisions/0026-host-posture-and-the-identity-backup.md) — the trigger falls from 40 050 dead tuples to 2100 at the 200 000-row shape, so an acknowledged envelope's space is reclaimed in minutes rather than after the next forty thousand acknowledgements. Measured — §4. The queue deletes a row on every acknowledgement, and a queue that empties and then goes quiet is what a shutdown looks like from this host | 2026-09-06 (`backend/messaging/migrations/0006_reclaim_the_queue_promptly.py`) |
| `LimitCORE` on `chat.service`, `chat-maintenance.service` and `chat-backup.service` | 0, soft and hard | the host's, which systemd's PID 1 raises to infinity for its children and `DefaultLimitCORE=` in `system.conf` then differs on between distributions and between releases of one | [0026](decisions/0026-host-posture-and-the-identity-backup.md) — a core dump of any of the three is its whole address space on disk: the JWT signing key, the Django secret key, the TURN shared secret, the database and Redis passwords, and whatever routing metadata, ciphertext or identity rows are in flight. `ProtectSystem=strict` does not stop one, because the kernel writes it wherever `kernel.core_pattern` points | 2026-09-06 (configured, `backend/ops/systemd/`; pinned by `core/tests/test_settings_posture.py`) |
| `ssl_session_tickets`, `ssl_session_cache` | `off` and `off` | `on`, and `none` — which stores nothing but still advertises resumption to the client | [0026](decisions/0026-host-posture-and-the-identity-backup.md) — a session ticket key is a long-lived symmetric secret in nginx worker memory, rotated only on reload, that opens the resumption state of every session it covers. The cost is one full handshake per connection, which this deployment can afford in a way a public web host cannot: one Android client holding one long-lived socket, not a browser opening a connection per asset | 2026-09-06 (configured, `backend/ops/nginx/chat.orviniq.com.conf`; pinned by `core/tests/test_settings_posture.py`) |
| `ruff` line length | 90 | 88 | [0013](decisions/0013-pytest-and-ruff-as-the-test-and-lint-stack.md) | 2026-09-03 |

## 3. Scale facts

| Object | The number | How the measurement ran | verified |
|---|---|---|---|
| Python | 3.12.10 | `backend/.venv/bin/python -V` | 2026-09-03 |
| Django | 6.0.7 | `django.get_version()` | 2026-09-03 |
| FastAPI / Starlette / Pydantic | 0.141.1 / 1.6.0 / 2.13.5 | `requirements/prod.txt` | 2026-09-04 |
| PyJWT / psycopg-pool / python-multipart | 2.13.0 / 3.3.1 / 0.0.32 | `requirements/prod.txt` | 2026-09-04 |
| uvicorn / uvloop / httptools / websockets | 0.52.4 / 0.22.1 / 0.8.0 / 17.1 | `requirements/prod.txt` | 2026-09-04 |
| redis-py | 8.0.1 | `requirements/prod.txt` | 2026-09-04 |
| httpx, cryptography (test only) | 0.28.1, 49.0.0 | `requirements/dev.txt` | 2026-09-04 |
| hypothesis, pytest-cov, coverage (test only) | 6.167.1, 7.1.0, 7.16.0 | `requirements/dev.txt`; arrived in run 12 per [0013](decisions/0013-pytest-and-ruff-as-the-test-and-lint-stack.md). `hypothesis` and `coverage` ship platform wheels rather than pure-Python ones, so the pins carry the hash of every file of the version, not only the developer machine's | 2026-09-04 |
| Pinned production distributions | 30, down from 42 when Channels, daphne and the sixteen packages only they pulled in left. The row read 29 before this run and was off by one against the command beside it | `grep -cE '^[a-zA-Z0-9._-]+==' requirements/prod.txt` | 2026-09-04 |
| jsonschema (test only) | 4.26.0, with `attrs` 26.1.0, `referencing` 0.37.0, `jsonschema-specifications` 2025.9.1 and `rpds-py` 2026.6.3 as its closure | `requirements/dev.txt`; arrived in run 13 for the contract suite, which holds every response to `backend/openapi.json` under the 2020-12 dialect the document declares. `rpds-py` ships platform wheels, so its pin carries the hash of every file of the version | 2026-09-04 |
| Pinned development distributions | 22, from 17 when jsonschema and its four dependencies arrived | `grep -cE '^[a-zA-Z0-9._-]+==' requirements/dev.txt` | 2026-09-04 |
| Project apps | 7 — `core`, `accounts`, `devices`, `vault`, `messaging`, `attachments`, `realtime`. It was 8 until phase 10 run 08 deleted the migrations-only `voicerooms` package | `INSTALLED_APPS` | 2026-09-06 |
| Project models | 10 — `accounts.User`, `accounts.ProfileBlob`, `devices.Device`, `devices.UserIdentity`, `devices.OneTimePrekey`, `devices.PqOneTimePrekey`, `devices.DeviceLogRecord`, `messaging.QueuedEnvelope`, `attachments.Attachment`, `vault.KeyBackup`. It was 11 while `voicerooms.Room` still had a class, though its table had already been dropped | `django.apps.apps.get_models()` filtered to the project apps | 2026-09-06 |
| Migration files | 19 — one `0001_initial` for each of the five apps that own a table, per [0009](decisions/0009-regenerate-the-initial-migrations.md), and fourteen of the append-only era: the retention index, the three activity-date retirements, the four-step move of the queue to a day, the autovacuum parameters, and the six of phase 10 run 08 that drop the orphaned columns and the hour index. The row read 7 and was stamped before phases 8 and 9 | `git ls-files 'backend/*/migrations/0*.py' \| wc -l`. Run over the working tree instead and `backend/.venv` adds 23 of Django's own | 2026-09-04 |
| Tracked Python files | 248, from 235 at the phase-9 merge. Phase 10 run 08 removed the four files of the `voicerooms` package and added the six migrations that drop the orphaned columns; the rest of the rise is the test files below. Every `.py` of this repository is under `backend/` — the count over the whole tree and the count over `backend/` are the same number | `git ls-files '*.py'` | 2026-09-06 |
| Test files | 124, from 120 at the phase-9 merge | `git ls-files 'backend/*/test_*.py'` | 2026-09-06 |
| Tests collected | 3485, plus 90 subtests. Phase 10 run 08 is **+38** against the 3447 at its merge base: the contract step's own gates, the deploy-order gate on a column removal, the rate limiter's two split-pair regressions and the mid-pipeline refusal, the Redis posture class, the at-rest quarantine gates, the rendered-panel exposure gates, and the three that hold `API_CHANGES.md`'s surface count to the generated document. Earlier figures, oldest first: 1293 before run 12, 2571 after it, 3558 after run 13, 3575 after run 14, 3185 at the phase-7 merge base, 3230 at the phase-7 merge, 3447 at the phase-9 merge | `pytest --collect-only -q` for the count, `pytest -q` for the subtests | 2026-09-06 |
| The OpenAPI document | 32 operations over 28 paths, 55 components, 128 004 bytes at `backend/openapi.json`. It read 32 over 27 paths at phase 5; phase 6 run 02 removed the four room operations across three paths, phase 7 added `POST /api/v1/me/relay`, phase 8 added `POST /api/v1/peers` and `GET /api/v1/config`, and this run added `DELETE /api/v1/me` with the one `EraseIn` component | `python manage.py openapi`, then `wc -c` | 2026-09-06 |
| URL routes declared | 1 `path()` entry across the `urls.py` files — the admin — plus `staticfiles_urlpatterns()` under `DEBUG`, 32 FastAPI method-and-path pairs over 28 distinct paths, and one WebSocket route at `/ws` with no HTTP method. FastAPI adds none of its own in any mode: [0020](decisions/0020-one-android-client-and-no-browser-surface.md) closed `/openapi.json`, `/docs`, `/docs/oauth2-redirect` and `/redoc` | `grep -rhn "path(" --include='urls.py' . \| wc -l`, and `core/tests/test_route_table.py` for the FastAPI table | 2026-09-05 |
| Project apps | `api/` is a Python package and not an installed app: it holds no model and appears in no `INSTALLED_APPS` | `INSTALLED_APPS` | 2026-09-04 |
| Production hardware | 1 vCPU, 1 GB RAM, single VPS | operator statement; no host metric exists yet | 2026-09-03 |
| Accounts, devices, groups | 0 accounts in production; the band caps the design at fewer than 50 accounts, at most 10 devices for each account, and at most 50 members in a group | pre-launch; the caps are the stated scale band, not a measurement | 2026-09-03 |
| Scale band | Band 0, pre-launch | no production traffic exists | 2026-09-03 |

The row-count rows a live deployment would carry do not exist yet. Read the
absence of a number as unknown, never as small: `messaging.QueuedEnvelope` is the
table whose size follows traffic, and nothing has measured it.

## 4. Measured operations

| Operation | The duration | The conditions of the run | verified |
|---|---|---|---|
| Full test suite, VPS | 2302.3 s (38 m 22 s), 3484 passed and 2 failed, 90 subtests, coverage 99.97 against the floor of 98 | The deployment host, `pytest --cov --cov-branch` under CPython 3.14.4, PostgreSQL 18 and Redis 8, venv activated and `TMPDIR` on real disk. **Both failures were defects of the suite that this release fixes, and neither is a property of the interpreter or the database**: `test_collectstatic_can_run` matched the string `0 static files`, which is what a populated `static_root` reports, and `test_an_authentic_register_token_is_a_scope_refusal_and_not_a_forgery` read a register token built at import time whose ten-minute life ends long before a 38-minute run reaches it. Both were proven separately — the second against a token aged 38 minutes, which answers `401 invalid_token` where a fresh one answers the `403 scope_forbidden` the test asserts. The host is 1 vCPU against the developer machine's, which is the whole of the 10× | 2026-09-07 |
| Full test suite | 211.0 s, 3485 passed, 90 subtests | Developer machine, `pytest --cov --cov-branch` against the floor of 98, native PostgreSQL 16 and Redis 7 on loopback, random order (`pytest-randomly` seed reported per run), nothing else touching the test database. This figure carries the coverage tracer; a bare `pytest -q --no-cov` over the same tree is 176 s. It read 238.1 s over 3447 tests at the phase-9 merge, 205.3 s over 3230 until phase 8, and 189.6 s over 3575 at phase 5 with no tracer | 2026-09-06 |
| Full test suite, second order | 173.0 s, 3230 passed, 90 subtests | The run inside the offline rehearsal's own venv, which is a second interpreter, a second install of the pinned set and a different `pytest-randomly` order, with no coverage tracer. Same code, same database, same result — which is the order-independence check and the offline gate in one | 2026-09-05 |
| Branch coverage | 99.97 percent — 3033 statements, none missed; 440 branches, 1 partial. The single partial branch is `core/panel.py` 47->exit, which is provably unreachable: `storage_label`'s loop cannot exit normally because its last arm always returns. The statement count was 2921 at the phase-9 merge and 3153 before phase 6; phase 10 run 08 added the audit gates and removed the `voicerooms` package | `pytest --cov --cov-branch`, configured in `backend/pyproject.toml`; `migrations/`, `tests/`, `manage.py` and `devices/vectors/generate.py` are excluded per run 12's decision 4, and `.venv/` and `vendor/` because they are not project code. **The floor is 98 from phase 10 run 08**, raised from the 95 run 13 set, in `backend/pytest.ini`, the CI test job and the `backend/pyproject.toml` comment ([0013](decisions/0013-pytest-and-ruff-as-the-test-and-lint-stack.md)) | 2026-09-06 |
| Gateway suite against a real Redis bus | 17.2 s, 147 passed | Developer machine, `pytest realtime/ -q -p no:randomly`. Every socket test drives the composed ASGI application on the test's own event loop and fans out through Redis publish and subscribe; three of them run a real uvicorn on an ephemeral port | 2026-09-04 |
| The regenerated migration history against the one it replaced | The same schema: 104 columns, 47 indexes and 61 constraints, identical name for name and definition for definition. The one difference is the physical column order of `devices_device`, where the four columns the old history appended now sit in model order | Both histories applied to a scratch database of their own on the developer machine, then `information_schema.columns`, `pg_indexes` and `pg_constraint` dumped from each and diffed. The `AddField` for `Device.refresh_generation` this row used to time is part of `devices.0001_initial` now and no longer runs against a populated table | 2026-09-04 |
| Every `0001_initial`, forward and back | Every statement is a `CREATE TABLE`, a `CREATE INDEX` or an `ALTER TABLE … ADD CONSTRAINT` against a relation the same migration created, so each ACCESS EXCLUSIVE lock is on a relation no other session can name. Every app reaches `zero` again, `accounts` cascading through `admin` on the way | `python manage.py sqlmigrate <app> 0001` read statement by statement, and `core/tests/test_migrations.py` for the replay | 2026-09-04 |
| `messaging.0002_index_the_retention_filter` | One statement, `CREATE INDEX CONCURRENTLY "ix_queue_queued_hour" ON "messaging_queuedenvelope" ("queued_hour")`, which takes SHARE UPDATE EXCLUSIVE and blocks neither reads nor writes. The migration is `atomic = False`, and its SQL carries no `BEGIN`. The index is 1544 kB at 200 000 rows | `python manage.py sqlmigrate messaging 0002`, and `core/tests/test_migrations.py`, which reads the same output against the recorded lock class | 2026-09-04 |
| `messaging.0003_expand_the_queue_to_a_day` | Three statements in one transaction, each a catalogue write on `messaging_queuedenvelope`: `ADD COLUMN "queued_day" date DEFAULT '<the migration's day>'::date NOT NULL`, the `DROP DEFAULT` Django emits behind it, and `ALTER COLUMN "queued_hour" DROP NOT NULL`. One ACCESS EXCLUSIVE hold for the length of the three, measured from `pg_locks` while the transaction holds it. The `ADD COLUMN` rewrites nothing: PostgreSQL 11 and later store the evaluated default in `pg_attribute.attmissingval`, measured at 1.7 ms against a 200 000-row, 111.6 MB copy with `relfilenode` unchanged either side. `DEFAULT CURRENT_DATE` and `DEFAULT (STATEMENT_TIMESTAMP())` behaved identically at 0.2 ms | `python manage.py sqlmigrate messaging 0003`, `core/tests/test_migrations.py` for the lock probe, and a `relfilenode`-before-and-after probe on a seeded scratch table for the rewrite | 2026-09-06 |
| `messaging.0004_backfill_the_queued_day` | No SQL at all: one `RunPython` that updates 1000 rows at a time, each batch in its own transaction, taking `ROW EXCLUSIVE` — the lock every INSERT, UPDATE and DELETE already holds. `atomic = False` is what makes the per-batch commit possible, and the whole file carries no schema operation for that reason | `python manage.py sqlmigrate messaging 0004`, which prints "THIS OPERATION CANNOT BE WRITTEN AS SQL", and the four backfill tests in `core/tests/test_migrations.py` | 2026-09-06 |
| `messaging.0005_index_the_day_filter` | One statement, `CREATE INDEX CONCURRENTLY "ix_queue_queued_day" ON "messaging_queuedenvelope" ("queued_day")`, which takes SHARE UPDATE EXCLUSIVE and blocks neither reads nor writes. The migration is `atomic = False`, and its SQL carries no `BEGIN`. The index is 1456 kB at 200 000 rows, beside the 1272 kB of `ix_queue_queued_hour`, which is maintained on every insert until run 08 drops the column | `python manage.py sqlmigrate messaging 0005`, and `pg_relation_size` on the seeded copy | 2026-09-06 |
| `messaging.0006_reclaim_the_queue_promptly` | One statement, `ALTER TABLE "messaging_queuedenvelope" SET (autovacuum_vacuum_scale_factor = 0.01, autovacuum_vacuum_threshold = 100)`, which takes SHARE UPDATE EXCLUSIVE on that table and nothing else — it blocks no reader and no writer, only a concurrent DDL or vacuum on the same table. It rewrites nothing: `relfilenode` unchanged either side against a 200 000-row copy. `RESET (…)` takes the same lock and restores the cluster defaults, which is what makes the migration reversible rather than merely un-appliable | `python manage.py sqlmigrate messaging 0006`, `core/tests/test_migrations.py` for the lock probe, and a `relfilenode`-before-and-after probe on a seeded scratch table | 2026-09-06 |
| What the queue's autovacuum trigger buys, stock against tuned | Two 200 000-row tables of the queue's shape, 3000 rows deleted from each. Stock (trigger 50 + 0.2n = 40 050): **0 autovacuums and 3000 dead tuples still present at t+150 s**. Tuned (trigger 100 + 0.01n = 2100): **one autovacuum by t+60 s, dead tuples back to 0**. Relation size unchanged at 14 286 pages on both, which is the honest limit of what this buys: the vacuum returns the space to the free space map and does not truncate the file, so what the parameters bound is when the space becomes *reusable*, not when the bytes are gone. They are overwritten by the insert that takes the space | PostgreSQL 16.14 on the developer machine, `autovacuum_naptime` at its 60 s default, `pg_stat_user_tables.n_dead_tup` and `autovacuum_count` sampled every 15 s for 180 s | 2026-09-06 |
| `ALTER TABLE … DROP COLUMN`, the second step of every column removal | **0.065 ms**, and ACCESS EXCLUSIVE on the named table and nothing else, read from `pg_locks` inside the transaction. `pg_relation_filenode` is unchanged either side and the heap stays 223 MB, so it rewrites nothing: PostgreSQL marks the attribute dropped in `pg_attribute` and the index on the column goes with it. **It removes the column and not the values** — a marker written into a column was still present in `get_raw_page()` after the drop and absent after `VACUUM FULL`, so a drop is a schema change and never an erasure. Free on this deployment because [0009](decisions/0009-regenerate-the-initial-migrations.md) makes create-and-migrate-once the supported path, so no environment carries a row written before the retirement | A 200 000-row, 245 MB copy of the queue's shape on the developer machine, PostgreSQL 16.14, plus a `pageinspect` probe on a two-row table | 2026-09-06 |
| `ALTER TABLE … DROP CONSTRAINT` on a foreign key | ACCESS EXCLUSIVE on **both** sides — the referencing table and the referenced one — because a foreign key is a property of each. Measured on a copy of the attachment and account shape; it is what puts `accounts_user` in the `BLOCKING_LOCKS` row for `attachments.0002_drop_the_uploader_link`. Dropping a constraint validates nothing, so unlike adding one it never scans | The same machine, `pg_locks` read inside the transaction | 2026-09-06 |
| `DROP INDEX CONCURRENTLY` against the plain form | The concurrent form holds **SHARE UPDATE EXCLUSIVE on the table and on the index** while it waits for the transactions that can still be reading through it, and blocks no reader and no writer. The plain `DROP INDEX` takes **ACCESS EXCLUSIVE on the table**. Both read from `pg_locks`; the concurrent one from a second session while the drop waited on an open transaction | The 200 000-row copy, PostgreSQL 16.14 | 2026-09-06 |
| The six migrations of phase 10 run 08 | Each recorded lock re-derived from `pg_locks` rather than read off the record: `accounts.0003` and `vault.0003` one table each, `devices.0003` three tables, `attachments.0002` two (with the foreign key's index dropped with the column), `messaging.0008` one — all ACCESS EXCLUSIVE for a catalogue write. `messaging.0007` is `atomic = False` and measured separately above. The whole history then replayed forward, every app to zero, and forward again: **91 columns either side, identical, and no orphaned column returns** | A scratch database of its own, created and dropped | 2026-09-06 |
| Peer state against the peer count | **5 queries flat at 1, 8 and 64 peers** — the cap is 64 — at 6.9 ms, 7.1 ms and 13.1 ms. The conditional form, every request `etag` still matching, is also 5 queries and faster at 64 (8.4 ms) with 64 of 64 answered `unchanged` | `CaptureQueriesContext` over the composed application, transaction statements excluded, on the developer machine | 2026-09-06 |
| Retention sweep on `queued_day`, re-measured | **5.58 s** for 100 000 expired of 200 000 (245 MB), 100 batches; **0.31 s** with nothing expired. The `queued_hour` figures this replaces were 5.95 s and 0.33 s for the same shape, so moving the filter to a day cost nothing. Both retention queries `Index Scan using ix_queue_queued_day`; one batch of 1000 is 224 buffers and 0.215 ms. The table now maintains **three** indexes — `ix_queue_queued_day` 1904 kB, the primary key 11 MB, `uq_queue_device_seq` 20 MB — where it maintained four, because `ix_queue_queued_hour` left with its column | `time manage.py prune` against a freshly seeded copy of the recorded shape, and `EXPLAIN (ANALYZE, BUFFERS)` | 2026-09-06 |
| Redis round trips of one request | **1 for any throttled route** — a single pipeline carrying `INCRBY` and `EXPIRE … NX`. It was 1 in steady state and 2 on the first hit of a window until phase 10 run 08 pipelined the pair; it is now 1 always. An attachment upload is **2**, the limiter's pipeline and the allowance's, first upload of a day and every later one alike | Counted at `Redis.execute_command` and `Pipeline.execute` through the composed application | 2026-09-06 |
| What a failing statement writes to the PostgreSQL log | At the stock `log_min_error_statement = error` and `log_error_verbosity = default`, one duplicate insert on a mailbox-shaped table wrote three lines: the `ERROR` with the constraint name, a `DETAIL` reading `Key (device, seq)=(11111111-…, 7) already exists.`, and a `STATEMENT` carrying the whole `INSERT` with its blob. Under `panic` and `terse` the same insert wrote the `ERROR` line alone | PostgreSQL 16.14 on the developer machine, a scratch database with a `UNIQUE (device, seq)` table, both settings applied at session level and the server's own log file read either side | 2026-09-06 |
| The identity backup's dump, and its restore | The dump is the eight tables `backend/ops/backup/identity_backup.sh` lists, ordered `accounts_user`, `accounts_profileblob`, `devices_device`, `devices_devicelogrecord`, `devices_onetimeprekey`, `devices_pqonetimeprekey`, `devices_useridentity`, `vault_keybackup` — pg_dump's own foreign-key ordering for a `--data-only` dump, so it loads without deferring a constraint. 13 936 bytes for one account with one device. No `messaging_queuedenvelope`, `attachments_attachment`, `django_admin_log` or `django_session` row appears in it. Restored into a **separately migrated** scratch database with `ON_ERROR_STOP=on`: exit 0, the same `user_id`, the same device id, the one-time prekey of each kind, the device-log record, the profile blob, the identity and the key backup all present, and `check_password` still true against the original password — the same `False` from `authenticate()` on both sides, because the account is inactive in both. The queue and the attachment tables restored empty, which is the exclusion working. **The `age` hop was not exercised**: the developer machine carries no `age`, so the encryption and decryption steps have never run — AR-20 | Two scratch databases on the developer machine, `manage.py migrate` into each, the script's own `pg_dump` invocation, `psql -f`, and assertions through the Django ORM | 2026-09-06 |
| A rate-limit window rolling under a test | Observed twice across six full-suite runs, on two different tests: `test_a_throttled_answer_carries_the_envelope_and_echoes_no_input` and `test_the_surface_answers_every_status_the_document_declares[POST /api/v1/auth/register 429]`. Both spend a scope set to `1/min` and then assert on the refusal, and `api/ratelimit.py` keys its counter on `int(time.time()) // period` — a fixed calendar window — so two requests that straddle a boundary land under different keys and the second is served. Reproduced deterministically by stepping the limiter's clock 60 s between the two calls: the expected `429` came back `200`. Closed by the `one_rate_limit_window` fixture, which pins the clock `api.ratelimit` reads for the tests that make the assertion; the counter, its key and its refusal are unchanged, and `test_the_counter_lives_in_redis_under_a_key_that_names_the_window` deliberately does not take it | Developer machine. The failures came from `pytest -q --cov` and from the suite inside `ops/audit/offline_rehearsal.sh`'s own venv; the reproduction and the fix were driven by a throwaway probe under `core/tests/` | 2026-09-06 |
| Test-database teardown | warns on roughly one run in three: `database "test_chatapp" is being accessed by other users`, one session | Developer machine, `pytest -q` repeated. It is a teardown warning and never a failure; the suite is green either way, and CI is unaffected because each run builds a fresh database. Reproduced with `DB_POOL_MIN_SIZE=0`, so it is not the pool's idle connection but a connection a worker thread still holds when `destroy_test_db` runs | 2026-09-04 |
| Send fan-out | 4 queries of its own, at 1, 6 and 20 recipients alike, and 4 for ten envelopes to one mailbox: one locked liveness read, one aggregate of the undelivered bytes each target holds (the mailbox ceiling), one bulk counter update, one bulk insert. A batch that reaches only stale devices costs the liveness read alone. The authentication dependency adds one query to every route | `CaptureQueriesContext` over the composed application, transaction statements excluded, in `messaging/tests/test_query_counts.py` | 2026-09-04 |
| Prekey claim | 1 target read, 1 locked select per target device, and 1 delete for the batch — 3 queries of its own for one device with a pool of 1, 20 or 200 keys, and 8 for six devices. An exhausted pool costs one less, because nothing is deleted | `CaptureQueriesContext` over the composed application, transaction statements excluded, in `devices/tests/test_query_counts.py` | 2026-09-04 |
| Attachment upload | 1 query of its own — the insert — at 0 and at 25 attachments already held. It was 3 until [0025](decisions/0025-unlinked-attachments-erasure-and-day-granularity.md) removed the uploader's row lock and the `SUM` aggregate with the lifetime quota. **2 Redis round trips, first upload of a day and every one after it alike**: one pipeline for the limiter and one for the allowance, each carrying `INCRBY` and `EXPIRE … NX`. This row read "2 on the first upload of a day and 1 after it" and was stale — the allowance was pipelined when it was written, and phase 10 run 08 pipelined the limiter beside it. A refused upload is 0 queries of its own. The download is 1. The authentication dependency adds one query to every route | `CaptureQueriesContext` over the composed application, transaction statements excluded, in `attachments/tests/test_query_counts.py`; the round trips counted at `Redis.execute_command` and `Pipeline.execute` | 2026-09-06 |
| Offline install of the whole pinned set | 52 hash-verified wheels collected and installed with `--no-index`: 30 production distributions and 22 development ones, one wheel each, none of them a source distribution. The whole suite — 3230 tests — runs green inside the venv built from that set. **The script inherits the caller's PATH for a bare `python`**, which `ops/tests/test_sbom.py` shells out to; on a host whose only interpreter is `python3` the rehearsal reports two failures that are the environment and not the code, so run it with the project venv's `bin` on PATH as `backend/README.md` describes. Observed both ways on 2026-09-05 | `bash ops/vendor.sh && bash ops/audit/offline_rehearsal.sh` on the developer machine, which prints `PASS: offline rebuild + full suite green.` | 2026-09-05 |
| Hash-verified dependency install | 7 s to fetch and install the two added wheels | `pip install --require-hashes -r requirements/dev.txt` on the developer machine, online | 2026-09-03 |
| A request in flight when the process is stopped | With `--timeout-graceful-shutdown 2`: the send failed 2.3 s after `SIGTERM` and the client read `HTTP/1.1 500 Internal Server Error`; the process exited at 2.3 s. With the window at 10 s: the body completed and the client read the route's real status, `HTTP/1.1 401 Unauthorized`; the process exited 6.4 s after the signal, when the request finished. A drain window below the request in flight therefore reports a deploy as a fault of the API | A real uvicorn on the developer machine serving `config.asgi:application`, one `POST /api/v1/auth/login` whose declared body was trickled to completion 6.0 s after the signal. The unit's window is 130 s against the 120 s of `UPLOAD_DEADLINE_SECONDS` for the same reason at the real scale | 2026-09-05 |
| `Strict-Transport-Security` headers per response, at the edge | Before: 2 on a proxied admin response, 1 elsewhere. After: 1 on a proxied 200, 1 on a file served from disk, 1 on nginx's own `= /` 404, 1 on a 404 that matched no location, 1 on nginx's own 413 | A real nginx 1.31.2 on the developer machine, serving the committed site file over TLS against a stub upstream that sets the header exactly as Django's `SecurityMiddleware` does. `curl -I` per response class | 2026-09-05 |
| The per-location body caps, at the edge | A 16 000-byte `POST` to the admin path is `200` and a 17 000-byte one is `413`; the same 17 000 bytes to `/api/` is `200`. Before, both were `200` at the edge and the application refused the larger one after nginx had carried it to loopback. `/static/` carries `X-Content-Type-Options: nosniff`, which it did not before; the internal attachment location answers `404` to a direct request | The same nginx, same method | 2026-09-05 |
| The credential `POST /api/v1/me/relay` mints, against a real relay | Accepted. `backend/realtime/relay.py::mint` was called through `django.setup()` against the project's own settings, and the username and password it returned were handed to `turnutils_uclient -y -u … -w …`: the allocation completed and the client relayed its messages, exactly as the reference run did with `-W <secret>`, which lets coturn's own implementation compute the credential instead. The same username with one bit flipped in the first byte of the credential was refused with `ERROR Cannot complete Allocation`, so the HMAC is verified rather than ignored — which is what makes the accepted run mean something. The minted username had the published shape, `1788652236:UtT90LjVf69JOUkd1JDOzg==`. The two outcomes the runbook's step 8 check 10 documents were then produced against a relay posture mirroring the deployment, its own address denied as a peer: the minted credential reached `channel bind: error 403 ()`, the documented PASS, and a mismatched secret gave `ERROR Cannot complete Allocation`, the documented FAIL | A real coturn 4.17.2 on the developer machine, `use-auth-secret` with a static secret generated for the run and held only in a scratch directory, listening on loopback. Run against 4.17.2 because that is what the developer machine installs; the deployment installs 4.6.1, whose `README.turnserver` documents the identical construction — an expiry timestamp, a colon and any string, with the password base64 of HMAC-SHA1 over that username under the shared secret — so the result carries, and it has not been run against 4.6.1 | 2026-09-05 |
| A relay allocation against the committed coturn file, in three variants | With `denied-peer-ip=::` present, the bind to an IPv4 peer was refused: the client printed `channel bind: error 403` and exited non-zero, and the relay logged `A peer IP 192.168.0.102 denied in the range: ::`. With that line removed, the same bind succeeded: the client printed its `start_mclient: tot_send_msgs=<n>, tot_recv_msgs=<n>` summary and exited 0. With the host's own address denied in its place, only that address was refused, which is the proof of the fix phase 6 landed for AR-6 | A real coturn 4.17.2 on the developer machine, driven by `turnutils_uclient -y` against a listener on the machine's own LAN address, with the credential computed from a static secret generated for the run. The measurement instance wrote its log to stdout; the deployed configuration writes none (AR-15), so on the VPS the client output is the whole of the evidence. The 4.6.1 that Debian bookworm and Ubuntu noble install carries the identical matcher in `ns_turn_ioaddr.c` | 2026-09-05 |

| A `Range` request on an attachment download, at the edge | `206 Partial Content` with `Content-Range: bytes 10-19/65536` and exactly the ten bytes asked for, and the application's `Cache-Control: private, no-store` intact. The full response carries `Accept-Ranges: bytes`, plus an `ETag` and a `Last-Modified` nginx derives from the file, so a client can resume under `If-Range`. The application is untouched by any of it: it answers `200` with an empty body and the redirect header, and the internal location's static handler does the range | A real nginx 1.31.2 on the developer machine, the committed `/api/` and `/_protected_attachments/` locations against a stub upstream that sets `X-Accel-Redirect` exactly as `attachments/routes.py` does, `curl -H 'Range: bytes=10-19'` | 2026-09-06 |
| Where a slow client's upload body is paid for | With `proxy_request_buffering on`, the upstream saw the request headers at 5.07 s and read the whole body in the same instant — nginx had absorbed all of it first. With it off, the upstream saw them at 1.55 s and spent the next 3.5 s reading, inside its own deadline. This is what makes `UPLOAD_DEADLINE_SECONDS` a bound on the loopback hop rather than on the client's link | The same nginx, a 200 KiB body trickled from a raw socket in 8 chunks 0.5 s apart, against a stub upstream that timestamps the request line and the completed body | 2026-09-06 |
| Retention sweep, 99 962 expired envelopes of 200 000 | 5.95 s wall, 100 batches of 1000 | `time manage.py prune` against the seeded copy below. Includes interpreter and Django start-up | 2026-09-04 |
| Retention sweep, nothing expired | 0.33 s wall, almost all of it interpreter and Django start-up. The envelope probe itself is 2 buffers and 0.027 ms | The same command against the same copy with nothing past its TTL | 2026-09-04 |
| Live push of one maximum batch | 131 ms of event-loop CPU: `json.dumps` of 256 payloads carrying a 349 528-character base64 blob each. The 53 ms of `base64.b64encode` that used to precede it is gone — the string the sender sent is handed through instead — and the 256 publishes are one pipelined round trip rather than 37.1 ms of sequential ones | `time.perf_counter` around the encode-and-serialize loop, 256 × the 262 144 bucket, on the developer machine | 2026-09-04 |
| Redis fan-out, sequential against pipelined | 256 small publishes: 37.1 ms against 1.5 ms. 500 (the presence-target ceiling): 55.9 ms against 2.5 ms. `INCR` then `EXPIRE` as two round trips against one pipeline: 0.22 ms against 0.17 ms per request, which is why the rate limiter was left alone | `redis.asyncio` against the loopback instance on the developer machine | 2026-09-04 |
| The fan-out the bus actually issues, under its 1 MiB pipeline budget | A 50-member group at the 1024 bucket, 256 frames: 1 round trip, 2.6 ms, 0.9 MB of peak resident set. 500 presence frames: 1 round trip, 2.7 ms, 0.2 MB. The largest batch the body cap admits, 200 envelopes at the 262 144 bucket: 67 round trips, 129.2 ms — most of it `json.dumps` — and 11.1 MB. Unbounded, that last one held 178.3 MB against 37.8 MB for the same publishes issued one at a time, so the budget trades a round trip a frame down to a round trip a mebibyte without holding the fan-out twice | `realtime.bus.publish_many` against the loopback instance, `Pipeline.execute` counted and `ru_maxrss` read, on the developer machine | 2026-09-04 |
| One worker's resident set against its live socket count | 189.8 MB idle, 201.0 MB at 100 sockets, 214.4 MB at 250, 237.0 MB at 500 — 47 MB for the band's whole socket ceiling, about 94 kB each. It does not fall when the sockets close: the allocator keeps the pages | `ps -o rss=` against a real uvicorn on the developer machine, holding open `websockets` clients | 2026-09-04 |
| The concurrency limit against live sockets | With `--limit-concurrency 6`, six live WebSockets make every HTTP request answer uvicorn's own plain-text `503 Service Unavailable` — not this API's envelope. Both protocols share `server_state.connections`, the HTTP path sheds from the length of that shared set, and the WebSocket handshake never consults it, because the upgrade returns before the check. With the limit at 1024, 500 live sockets leave HTTP answering `200` | A real uvicorn on the developer machine, 6 then 500 `websockets` clients held open across an HTTP request | 2026-09-04 |
| Connection pool under contention | 60 concurrent drains through a pool of `max_size` 1 with a 1 s acquisition timeout: 60 × `200`, no timeout. The pool is not the binding constraint at these service times | `curl` × 60 against a real uvicorn on the seeded copy, `DB_POOL_MAX_SIZE=1 DB_POOL_TIMEOUT=1` | 2026-09-04 |
| A socket's bind against a send holding the same device row | 1.007 s before, 0.0005 s after. `send` holds `SELECT … FOR UPDATE` on each target device row to commit, and `_touch_active` wrote that row unconditionally on every bind; excluding the row that already says today leaves the statement count at one and takes no row lock when nothing matches | A thread holding the lock in the statement `send` uses, timed against `_touch_active` on the test's own connection, in `realtime/tests/test_bind_contention.py`. The wait mattered beyond one socket because a websocket scope enters no `ThreadSensitiveContext`, so every socket's ORM unit runs on one executor thread | 2026-09-04 |

### 4.1 The request rate of one worker

Closed-loop, one uvicorn worker with the production flags, real PostgreSQL 16 and
Redis 7 on loopback, throttle rates raised so the numbers are the routes rather
than the limiter, against the seeded copy below. **Measured on the developer
machine, which has more than one core**, so the generator and the ORM threads did
not contend with the event loop the way they will on the VPS: read the shape — a
knee, then a decline — rather than the absolute rate. Verified 2026-09-04.

| Route (queries) | c=1 | c=8 | c=32 | c=128 |
|---|---|---|---|---|
| `GET /health` (0) | 1894 rps, p50 0.51 ms, p95 0.66 | 1352 rps, p50 3.07, p95 3.93 | 956 rps, p50 24.3, p95 90.9 | 796 rps, p50 116, p95 444 |
| `GET /me/envelopes` (2, 137 kB body) | 358 rps, p50 2.69, p95 3.43 | 576 rps, p50 13.3, p95 18.5 | 577 rps, p50 53.5, p95 72.2 | 88 rps, p50 749, p95 4292 |
| `GET /users` (2) | 557 rps, p50 1.74, p95 2.10 | 1014 rps, p50 7.54, p95 12.1 | 360 rps, p50 61.3, p95 256 | 233 rps, p50 388, p95 1555 |
| `GET /me/devices` (4) | 409 rps, p50 2.42, p95 2.69 | 706 rps, p50 11.0, p95 14.6 | 366 rps, p50 55.5, p95 262 | 116 rps, p50 800, p95 2774 |

The knee is between 8 and 32 concurrent requests. Past it throughput falls and
p95 grows faster than concurrency, which is one event loop saturating. Every
response was `200`; nothing was shed and nothing reached its deadline.

**Re-run on 2026-09-06** on the same machine, after phase 10 run 08 pipelined the
rate limiter, four seconds per cell:

| Route | c=1 | c=8 | c=32 | c=128 |
|---|---|---|---|---|
| `GET /health` | 2147 rps, p50 0.46 ms, p95 0.48 | 1656 rps, p50 2.75, p95 3.12 | 1121 rps, p50 20.3, p95 77.8 | 550 rps, p50 166, p95 657 |
| `GET /me/envelopes` | 394 rps, p50 2.51, p95 2.68 | 653 rps, p50 11.9, p95 15.1 | 635 rps, p50 48.5, p95 62.8 | 100 rps, p50 786, p95 3725 |
| `GET /users` | 564 rps, p50 1.74, p95 1.90 | 1091 rps, p50 7.11, p95 10.3 | 399 rps, p50 56.1, p95 219 | 158 rps, p50 537, p95 2300 |
| `GET /me/devices` | 434 rps, p50 2.27, p95 2.49 | 720 rps, p50 10.8, p95 14.5 | 394 rps, p50 50.2, p95 241 | 103 rps, p50 720, p95 3376 |

The same shape at every cell — the knee between 8 and 32, then the decline — and
every response was `200` again. Each `c=1` cell is a little above the 2026-09-04
run, which is the direction removing one Redis round trip from the first request of
a window predicts; nothing here is a claim that the two runs were identical
conditions, only that the curve did not move.

### 4.2 Query plans

`EXPLAIN (ANALYZE, BUFFERS)` on a seeded copy of the band's shape: 50 accounts,
500 devices, 200 000 undelivered envelopes at the 1024 bucket (247 MB with its
indexes), 25 000 device-log records, 20 000 attachments, 50 000 admin audit rows.
Developer machine, PostgreSQL 16, verified 2026-09-04. A buffer is 8 kB.

| Query | Plan | Buffers | Time |
|---|---|---|---|
| The retention pass with nothing expired, as it ran before this run: a `MAX(seq)` aggregate over the whole expired set, with no index | `Parallel Seq Scan` over the whole table | 28 736 | 26.5 ms |
| The same aggregate, with the index | `Index Scan using ix_queue_queued_hour` | 2 | 0.012 ms |
| The retention probe with nothing expired, as the batched sweep ran it against `queued_hour` | `Index Scan using ix_queue_queued_hour`, `Limit` | 2 | 0.027 ms |
| One retention batch of 1000 ids, against `queued_hour` | `Index Scan using ix_queue_queued_hour`, `Limit` | 991 | 5.04 ms |
| The retention probe with nothing expired, as the sweep runs it now — [0025](decisions/0025-unlinked-attachments-erasure-and-day-granularity.md) moved the filter to `queued_day` | `Index Scan using ix_queue_queued_day`, `Limit` | 2 | 0.012 ms (re-measured 2026-09-06 on a fresh seed of the same shape) |
| One retention batch of 1000 ids, against `queued_day` | `Index Scan using ix_queue_queued_day`, `Limit` | 1 002 | 2.95 ms (same seed) |
| Expired attachments (20 000 rows, 9746 matching) | `Seq Scan` — 5.9 MB table, no index warranted | 272 | 1.39 ms |
| Expired audit rows (50 000 rows, 27 445 matching) | `Seq Scan` — 5.5 MB table, no index warranted | 468 | 7.48 ms |
| The drain page of a 400-row mailbox | `Index Scan using uq_queue_device_seq`, `Limit` | 19 | 0.10 ms |
| The drain page of a 32 768-row mailbox | the same index scan | 19 | 0.09 ms |
| The mailbox ceiling aggregate, 400-row mailbox | `Bitmap Heap Scan` on `uq_queue_device_seq` | 64 | 0.58 ms |
| The mailbox ceiling aggregate, 10 000-row mailbox | the same bitmap heap scan | 1 520 | 1.16 ms |
| The mailbox ceiling aggregate at the 32 MiB ceiling | `Parallel Seq Scan` — one device now holds 16% of the table, so the index stops being selective | 33 196 | 17.9 ms |
| The locked liveness read of 20 send targets | `LockRows` over a `Sort` over a hash join | 63 | 0.61 ms |
| The device-log keyset page | `Index Scan using uq_devicelog_user_seq`, `Limit` | 11 | 0.16 ms |
| The device-log head probe | `Index Only Scan Backward`, 0 heap fetches | 3 | 0.10 ms |
| The attachment quota aggregate, 400 held | `Bitmap Heap Scan` on the uploader index | 269 | 0.90 ms. **Historical**: [0025](decisions/0025-unlinked-attachments-erasure-and-day-granularity.md) removed the aggregate and phase 10 run 08 dropped the column and its index, so neither this query nor this index exists |
| The attachment quota aggregate at the 2 GiB quota ceiling (32 400 held) | `Seq Scan` — the table is narrow, so the whole scan is 5.4 MB | 694 | 1.82 ms |

Two of these grow with a ceiling rather than with the band, and both are recorded
as accepted risks with their triggers in
[`../../ACCEPTED_RISKS.md`](../../ACCEPTED_RISKS.md): the mailbox ceiling
aggregate (AR-7) and the attachment quota aggregate (AR-8). Neither is indexed
today, because at the band's real depths both are already cheap and an index on
either is maintained on the hottest write path in the system.

No restore drill, no failover drill, no rollback and no deploy has been timed.
Those rows stay absent until a drill produces them, and the absence is the fact —
never read it as a small number. `backend/ops/RUNBOOK.md` is the operator runbook
from 2026-09-05 (ADR 0015); its rollback has never been executed, which
[`../../ACCEPTED_RISKS.md`](../../ACCEPTED_RISKS.md) carries as AR-13 with the
trigger that produces the first of these rows.

## 5. Domain rules

| Rule | What imposes it | What breaks when the code ignores it | verified |
|---|---|---|---|
| The server holds no content-encryption key. Its only secrets are the TLS key, the JWT signing key, the Django secret key, and the TURN secret | The threat model in `backend/SECURITY.md`: an attacker with live root on the VPS reads everything the server holds | Every message ever sent becomes readable from one seizure | 2026-09-03 |
| The server verifies no cryptographic material. A length, bucket or version check is a malformed-input guard and never a security control | `backend/SECURITY.md`; the client half is `backend/CLIENT_CONTRACT.md` | A client skips its own verification because "the server checks it", and the server is the adversary | 2026-09-03 |
| Every stored ciphertext has an exact bucket length; an off-bucket payload is rejected with `400 bad_bucket` and no echo | `backend/core/buckets.py` and `backend/core/fields.py` | Ciphertext length leaks message length, and the padding buys nothing | 2026-09-03 |
| No plaintext, no content key, no sender column, no membership table, no recipient list, and no shared-blob link exists at rest | `backend/core/tests/test_seizure_guard.py` and `test_manifest.py` | The social graph survives a seizure even though the content does not | 2026-09-03 |
| No server-side message history exists. An envelope row dies on ack, and an undelivered envelope dies after `ENVELOPE_TTL_DAYS` | The threat model; `messaging.QueuedEnvelope` | The server becomes the archive the design exists to avoid | 2026-09-03 |
| Every mailbox holds at most `MAILBOX_MAX_BYTES` of undelivered bytes and every device-list log at most `MAX_DEVICELOG_RECORDS` records | `messaging.services.send` and `devices.services.append_log`, under the locks they already hold; `messaging/tests/test_send_fanout.py` and `devices/tests/test_device_log.py` | Any member can address any mailbox and grow their own log without bound, and the rows of a mailbox carry no sender: one member fills the disk and the operator cannot name them | 2026-09-04 |
| No identifier, blob, token, password, address or request path reaches a log line, and no access log exists | The per-app `test_log_silence.py` suites and `backend/ops/audit/log_silence.py` | The log becomes the social graph that the schema refuses to hold | 2026-09-03 |
| Volatile data never touches disk: signals, rate counters and lockout state | Redis runs with `save ""` and `appendonly no` | A seized disk yields the traffic pattern | 2026-09-03 |
| No runtime foreign dependency exists; the system runs through a total national internet shutdown | The operating environment | The platform stops working at the moment it is most needed | 2026-09-03 |
| The server keeps no group state, no roster, and no group key | [ADR 0001](decisions/0001-pairwise-double-ratchet-group-fan-out.md) | A seizure yields the membership of every group | 2026-09-03 |
| No token is stored. Revocation is `Device.token_generation`, one integer on the device row; `Device.refresh_generation` was dropped by phase 10 run 08, so no column of the refresh token it counted survives | [ADR 0023](decisions/0023-one-device-bound-session-token.md), superseding [ADR 0006](decisions/0006-device-bound-tokens-on-pyjwt.md); `core/tests/test_manifest.py` fails on a `token`, `jti` or blacklist column | A token table is a per-device login record at rest, which is the login history a seizure would otherwise yield | 2026-09-05 |

## 6. The seam between the two runtimes

FastAPI runs beside Django in one process. These are the facts of that seam, each
one discovered from the repository rather than assumed.

| Fact | The value here | verified |
|---|---|---|
| Seam shape | Same-process mount. FastAPI is the root ASGI application for every scope; the Django ASGI application is the router's `default`, so it answers every path no FastAPI route claims. A websocket scope that no route claims is refused with a close rather than the JSON `not_found` envelope, which would need the websocket denial-response extension | 2026-09-04 (observed, `backend/config/asgi.py`, `backend/api/app.py`) |
| Database each runtime connects to | One: the `default` PostgreSQL database. There is no second database and no second connection string | 2026-09-04 (observed, `config/settings/base.py`) |
| DDL owner | Django. There is no second migration tool, and the FastAPI side declares no schema of its own | 2026-09-04 (observed, `*/migrations/`) |
| Writer of each table | The Django ORM, in every case. The FastAPI routes write through the same models, inside synchronous units of work, so the field defaults, the `auto_now` values and the signals all run | 2026-09-04 (observed, `accounts/services.py`, `vault/services.py`, `devices/services.py`, `messaging/services.py`, `attachments/services.py`) |
| Data access layer | The Django ORM only, through `api/orm.py`: one synchronous function for each unit of work, bracketed with `close_old_connections()` and run with `sync_to_async(thread_sensitive=True)` inside a per-request `ThreadSensitiveContext`. A websocket scope enters no such context, so the gateway's four units share the one process-wide thread-sensitive executor thread | 2026-09-04 (observed, `api/orm.py`, `api/middleware.py`, `realtime/auth.py`) |
| Credential at the seam | One HS256 bearer token, issued and verified only by `api/auth.py`. The FastAPI dependencies and the WebSocket gateway both call that module, so a token the HTTP surface revokes is dead on the socket, and the revocation reaches a live socket over the bus within the publish latency | 2026-09-04 (observed, `api/auth.py`, `realtime/auth.py`, `realtime/bus.py`; asserted by `accounts/tests/test_device_auth.py` and `realtime/tests/test_revoke_close.py`) |
| Broker | None. There is no task queue and no Celery application; the only scheduled work is the `prune` management command under a systemd timer | 2026-09-04 (observed) |
| Cache | One Redis instance, `REDIS_URL`. The rate limiter, the login lockout and the gateway's fan-out bus all use it, and every one of them reads strings through the redis client: the Django cache framework is not on Redis, so no value read from the instance is ever deserialized ([0018](decisions/0018-redis-is-authenticated-and-never-deserialized.md)). `api/redis.py` holds one `redis.asyncio` client for each running event loop, and the limiter and the bus share it, so one process draws one pool plus the one connection the subscription holds. The limiter keys its counters under `ratelimit:`, the lockout keys its counter and its flag under `lock:<surface>:` for each of the two password surfaces, and the bus publishes to `ws:dev:<device id>`, which is a channel rather than a key and stores nothing. There is one counter stack, and no scope is counted twice | 2026-09-05 (observed, `api/redis.py`, `api/ratelimit.py`, `realtime/bus.py`) |
| Process set | One uvicorn master with `WEB_CONCURRENCY` workers, default 1. Each worker serves HTTP and WebSocket alike and opens one Redis subscription connection of its own, so the subscription count tracks the worker count | 2026-09-04 (configured, `backend/ops/systemd/chat.service`) |
| Connection budget | `max_size` 16 per worker, so 16 × `WEB_CONCURRENCY` plus one for the maintenance timer while it runs: 17 at the default, against PostgreSQL's own default `max_connections` of 100. The VPS value of `max_connections` is not measured. `--limit-concurrency 1024` bounds **connections**, live WebSockets included, so it is sized for the device ceiling of the band plus a keep-alive HTTP connection for each of those devices, rather than for HTTP concurrency — the two share one knob and uvicorn offers no second. This row read 512 until 2026-09-05: the unit was raised to 1024 in phase 4 and the cell was not re-read | 2026-09-05 (observed, `config/settings/base.py`, `backend/ops/systemd/chat.service`) |
| Threads | One per in-flight HTTP request that touches the ORM: `api/middleware.ThreadSensitive` opens a `ThreadSensitiveContext` for each request, and asgiref builds a `ThreadPoolExecutor(max_workers=1)` for each context. A WebSocket scope enters no context, so every socket's ORM work shares the one process-wide thread-sensitive executor thread — which makes any wait inside a gateway unit a wait for every socket in the worker, and is why `_touch_active` writes only on the day the date changes (§4) | 2026-09-04 (observed, `api/middleware.py`, `asgiref.sync.SyncToAsync.__call__`) |
| Lifespan | uvicorn sends both messages. Nothing is built at startup all the same, because what the process holds is keyed by running event loop: the shared Redis client, the bus subscriber and its reader task are built on first use. The shutdown drains every live socket with `1012`, stops the subscriber, then closes the client, in that order. uvicorn closes those sockets at the signal rather than draining them, so the 130 s graceful window bounds the HTTP requests alone | 2026-09-05 (observed, `api/app.py`, `realtime/bus.py`, `realtime/gateway.py`) |
